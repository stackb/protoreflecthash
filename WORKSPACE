workspace(name = "com_github_stackb_protoreflecthash")

# NOTE: bazel is only being used to fetch protoc and to use the proto_library
# rule for generating a proto descriptor set of the test protos, for embedding
# into tests.
